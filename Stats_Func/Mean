
public class Mean {

    public static int main(double[] ar, int rowMin, int rowMax, int col){

        double total = 0;

        for(i=rowMin,i>rowMax,i++){
            total=total+ar[col,i];
        }

        return total/(rowMax-rowMin);
    }
}